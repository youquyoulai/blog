# 2026-04-29 工作记录

## Waline → Twikoo 迁移

- 导出 Waline 117 条评论（waline_comments.json）
- 部署 Twikoo Cloudflare Worker（twikoo-cloudflare 项目）
- Worker 地址：twikoo.pgoj.top（已绑自定义域名）
- D1 数据库名：twikoo，建好 comment/config/counter 三张表
- 写迁移脚本 _migrate_to_twikoo.js，生成 _twikoo_import.sql，117 条全部导入
  - 注意：Waline 时间字段有两种格式（空格分隔 和 ISO 8601），都要兼容处理
- 博客模板 posts/single.html 改为 Twikoo 前端（twikoo.all.min.js v1.6.44）
- hugo.toml 改为 [params.twikoo] envId = 'https://twikoo.pgoj.top'
- Twikoo JS 存放：static/js/lib/twikoo.all.min.js
- wrangler.toml 设置 TWIKOO_AUTH_TOKEN 管理密码
- 初次部署遗漏 path 参数，导致评论区不显示，已补上
- 迁移完成，评论功能正常

## 侧边栏优化

- 新增工具栏 widget（邮箱/RSS/站点地图/暗色模式切换），桌面端卡片式样式
- 站点地图链接指向 /sitemap.xml，图标用文档列表样式（避免与顶部搜索重复）
- 图标尺寸从固定 px 改为 em 相对单位，自适应大小
- 移动端侧边栏 widget 统一为卡片式（之前是透明无阴影）
