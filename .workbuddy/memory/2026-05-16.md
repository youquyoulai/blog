# 2026-05-16 工作记录

## 修复搜索功能（searchindex.json 缺失）
- 问题：搜索页报"搜索索引加载失败"，本地和线上都复现
- 根本原因：hugo.toml 已配置 `outputs.home=['HTML','RSS','JSON']` 和 `outputFormats.JSON.baseName=searchindex`，但缺少对应的模板文件 `layouts/index.json`，Hugo 不生成 searchindex.json
- 修复：新建 `layouts/index.json` 模板，遍历 posts section 所有文章，输出 title/url/date/summary/categories/tags 字段
- 验证：hugo build 后 `public/searchindex.json` 成功生成
