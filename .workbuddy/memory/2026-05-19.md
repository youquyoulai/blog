# 2026-05-19 工作记录

## weisaygrace 主题搜索框修复
- 文件：`themes/weisaygrace/layouts/partials/header.html`
- 问题1：label 的 `sr-only` 类未在 CSS 中定义，导致"搜索"文字显示出来（加上 placeholder 就有两个"搜索"）
- 问题2：搜索按钮用内联 style 定位在 left:3px，背景 #ed5565 红色
- 修复：移除 `<label>` 标签改用 `aria-label`，将按钮改为 `right:4px` 定位，背景改为 `transparent`，图标颜色改为灰色 #999，input padding 改为右侧留空

## weisaygrace 主题全面优化（2026-05-19）
- Waline 评论：CSS/JS 改为 Hugo 资源管道（`resources.Get` + `minify` + `fingerprint`），Waline 3.x 文件下载到 `static/js/waline/`
- 添加 `og:locale zh_CN` 元标签
- 暗色 CSS 改为 `<link rel="preload" as="style">` + `onload` 切换，避免渲染阻塞
- 清理未使用 CSS：`.vip*`(12处)、`.com-level`(3处)、`.article-mostactive`(1处)、`.article-404`(1处)、`.pj-reply`(1处)，共节省 ~677 bytes
- 简化冗长选择器：`.main .article .article-content` -> `.article-content`，Waline 深层嵌套简化，共节省 ~766 bytes
- CSS 总计：54630 -> 53187 bytes（压缩 2.6%）
- 主题使用 Waline（非 Twikoo），Waline serverURL = 'https://waline.pgoj.top'

