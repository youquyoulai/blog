# 2026-05-28 工作日志

## Admin 后台多 section 支持 + 数学题解 section

- **Worker API 通用化** (`static/admin/worker.js`)：将硬编码的 `content/posts` 改为动态 section 参数，所有文章 CRUD 通过 `?section=` 查询参数或 body 中的 `section` 字段决定操作目录，不传默认 `posts`，向后兼容
- **Admin 前端** (`static/admin/index.html`)：文章列表新增 section 下拉选择器（博客文章/数学题解），切换自动重新加载
- **新增 `content/math/`**：数学题解 section，含 `_index.md`（地图页）和示例文章
- **Hugo 兼容**：`mainSections = ['posts']` 不变，math 不出现在首页和主 RSS 中，自动拥有独立 RSS `/math/feed.xml`
- **待部署**：worker.js 需手动上传到 Cloudflare Workers；git push 需用户手动执行（凭证管理器在沙箱不可用）

## 部署与修复

- Worker 通过 Cloudflare Dashboard 部署完成（blog-admin-api → 快速编辑 → 粘贴代码 → 部署），新代码已生效
- 修复导航栏出现 "shuxue" 分类：`layouts/partials/header.html` 添加 `{{ if ne .Page.Title "shuxue" }}` 过滤
- 修复首页 RSS 包含 math 文章：`layouts/_default/rss.xml` 添加 section 过滤逻辑
- git push 成功，commit ec317eb，Cloudflare Pages 自动构建部署

## 数学 section 分类/标签预设

- **Worker** (`static/admin/worker.js`)：`getTaxonomies` 添加 `SECTION_TAXONOMY_PRESETS`，math section 预设分类 `math-solutions`（数学题解）、`exam-analysis`（试题评析），标签 `gaokao-math`（高考数学）、`mock-exam`（高三模拟）。扫描文章时合并预设（count=0），目录不存在时回退返回预设
- **Admin 前端** (`static/admin/index.html`)：分类/标签 Tab 添加独立的 section 下拉选择器（`taxSectionSel`），不再依赖文章 Tab 的 section；`_catDisplayNames` 和 `_tagDisplayNames` 添加 math 中文名映射；`loadTaxonomyHints` 修复缓存（切换 section 后重新加载）
- **Hugo 模板**：`cat-names.html` 添加 math 分类映射；`tag-names.html` 添加 math 标签映射；`header.html` 导航栏过滤从单一 `shuxue` 改为 `slice` 排除列表（shuxue/math-solutions/exam-analysis）
- commit `57634e4`，待 push 部署
