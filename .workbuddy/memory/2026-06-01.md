
# 2026-06-01 工作记录

## 博客后台管理重构

### 1. 文章页面两级文件夹结构
- 将 `loadPosts()` 从单层月份文件夹改为两级结构：年文件夹 → 月文件夹 → 文章列表
- 通过 `prefix` 参数驱动三层导航状态机：`''`（根）→ `'YYYY/'`（年级）→ `'YYYY-MM/'`（月级）
- 前端通过正则匹配 `yearMatch` / `monthMatch` 区分状态，API 接口零改动
- 备份文件：`index.html.bak2`（重构前完整备份）

### 2. UI 全面重构（WordPress/Typecho 风格）
- 将单文件后台 `static/admin/index.html`（约 2115 行）从简单布局重构为专业后台风格
- **新布局结构**：
  - 左侧固定侧边栏（深色 `#1e2433`，220px 宽）
  - 顶栏（白色，56px 高，含汉堡菜单 + 面包屑标题 + 操作按钮）
  - 主内容区（`margin-left: 220px`，浅灰背景）
- **响应式设计**：移动端侧边栏 `transform: translateX(-100%)` 隐藏，汉堡按钮触发 `.open` 类
- **CSS 设计令牌**：统一管理 `--sidebar-w`, `--topbar-h`, `--bg`, `--surface`, `--accent`, `--sidebar-bg` 等变量
- **保留所有 JS 逻辑**：只替换 CSS + HTML 部分（前 652 行），JS 部分（第 653 行起）完全保留
- **注入新 JS 函数**：
  - `toggleSidebar()` / `closeSidebar()`：移动端侧边栏控制
  - `switchTab()` 标题同步：切换 tab 时更新顶栏面包屑标题
- **备份链**：`index.html.bak2`（重构前）→ `index.html.bak3`（合并前）

### 3. 踩坑记录
- **文件路径问题**：在 bash heredoc 中写 Python 脚本到 `/tmp/`，Windows 环境下实际路径不对，改用 `e:/blog/static/admin/` 目录
- **Python 中文乱码**：bash heredoc 里运行 Python，print 输出中文乱码，但不影响文件操作
- **JS 注入失败**：`switchTab()` 中 `currentSection = name;` 缩进格式不匹配，导致正则注入失败，改用手动 Edit 工具直接注入
- **Token 显示位置**：`showApp()` 中旧 `currentTokenDisplay` 引用未清理，已修复为侧边栏底部的 `currentTokenDisplay`

### 4. 当前状态
- ✅ 两级文件夹结构已完成
- ✅ UI 重构已完成（侧边栏 + 顶栏 + 主内容区）
- ✅ 响应式布局已就绪
- ✅ 按钮美化已完成（评论/文汇/友链/编辑器等）
- ✅ SEO 优化（必应 Top Recommendations）已提交：
  - robots.txt 删除 /assets/* /static/* /tags/* /categories/*，开放标签/分类页爬取
  - title 模板：所有页均加 " - 站点名"，首页加副标题描述
  - description：文章页 truncate 160→200，列表页无描述时自动生成带作者名的兜底文案

### 5. 按钮美化（第二轮）
- **评论管理**：操作按钮改为 `.action-btn` 胶囊样式（批准/垃圾/回复/删除各带独立颜色和图标），状态标签改为圆角 badge
- **文汇 RSS**：列表项改为 `.list-card` 卡片样式，编辑/删除改为带图标的 action-btn，分类 badge 显示
- **友链管理**：分类头改为 `.link-cat-header` 带文件夹图标，链接项改为 list-card 样式，RSS 徽章，空状态虚线边框
- **编辑器**：保存/发布/删除按钮加图标，工具栏精简文字改为符号（H2/H3/代码等），图库选图按钮突出显示
- **通用**：刷新按钮加刷新图标，仅保存改为 `.btn-ghost`，评论筛选下拉选项优化文案
- **分页**：评论分页加左右箭头图标，页码显示改为胶囊 badge
- **CSS 新增类**：`.action-btn`（approve/spam/reply/del/edit）、`.list-card`、`.list-card-info/title/sub/badge`、`.link-cat-header`、`.btn-warning`、`.btn-xs`

### 6. 移动端评论卡片修复
- 问题：移动端评论内容一字一行（`.post-item` flex 布局被操作按钮挤压）
- 修复：在 `@media (max-width:600px)` 中添加 `.comment-card { flex-direction:column }` 和 `.comment-actions { flex-wrap:wrap; margin-top:8px }`，让内容区和按钮竖排
- 文件：`static/admin/index.html`
- 已推送至 GitHub（`984ac3c`）

### 7. 微信读书阅读统计页面集成

**需求**：将本地 reading.html（130KB）整合为 Hugo 博客独立页面 `/reading/`

**实现**：
- 新建 `content/pages/reading.md`（`layout: reading`，标题"阅读统计"）
- 新建 `themes/weisaygrace/layouts/pages/reading.html` 模板，适配主题 CSS 变量和暗色模式
- DATA 对象（103KB）通过括号匹配法从原始 HTML 提取到 `static/js/reading-data.js`
- 模板加载外部数据文件 + CDN Chart.js，包含统计卡片、柱状图、排行榜、偏好分类、书架网格（全部/已读完/在读筛选）

**UI 调整**：
- 各模块（标题、统计、分类分布、排行榜、偏好、书架）包装为 `.rp-card` 卡片样式，与文章列表 `.post` 卡片一致（白底、圆角、底部边框、阴影）
- 面包屑与内容间距从 2rem 缩到 0.25rem
- 导航：移除顶部导航菜单中的「阅读」，改到侧边栏工具栏（替换邮箱按钮，图标为书本 `icon-book`）
- 新增 SVG 图标 `icon-book`（baseof.html）
- 移动端汉堡菜单搜索框 padding 从 0.35rem 调到 0.875rem（与导航项对齐）

**书籍链接**：DATA 中 bookId（纯数字）不匹配微信读书网页版 reader ID 格式，改为搜索链接 `https://weread.qq.com/web/search/book/书名`

**文件**：
- `content/pages/reading.md`
- `themes/weisaygrace/layouts/pages/reading.html`
- `static/js/reading-data.js`
- `themes/weisaygrace/layouts/partials/header.html`（移除导航阅读项）
- `themes/weisaygrace/layouts/partials/sidebar.html`（工具栏替换邮箱为阅读）
- `themes/weisaygrace/layouts/partials/footer.html`（搜索框 padding）
- `themes/weisaygrace/layouts/_default/baseof.html`（新增 icon-book）

已推送至 GitHub

