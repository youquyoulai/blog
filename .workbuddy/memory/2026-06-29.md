# 2026-06-29 工作日志

## 推送 MathJax 3 支持

- `baseof.html` 添加 MathJax 3（`tex-svg.min.js`），全站支持 `$...$` 和 `$$...$$` 数学公式渲染
- 远程有新提交（3篇数学题解文章），先 `git pull --rebase` 后成功 push，commit `96fb1c4`
- `.rar` 压缩包未提交到 git

## math section 分类标签调整

- **分类三个**：`math-solutions`（数学题解）、`teaching-chatter`（数学闲话）、`exam-analysis`（试题评析）
- **标签两个**：`gaokao-math`（数学高考）、`mock-exam`（数学模拟）
- `header.html` + `footer.html` 导航菜单：排除列表只保留 `shuxue`，数学三个分类正常显示在菜单中
- `admin/index.html` `_catDisplayNames` 新增 `teaching-chatter` 映射
- `worker.js` `SECTION_TAXONOMY_PRESETS.math.categories` 新增 `teaching-chatter`
- commit `ced28b8`，已 push
