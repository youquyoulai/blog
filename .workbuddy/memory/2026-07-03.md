# 2026-07-03 工作日志

## 知识库改造：博客首页 → 分类卡片 + 三级层次结构

将 Hugo 博客从传统博客列表首页改造为知识库风格，支持「分类 → 人物 → 作品」三级层次。

### 新增文件
- `data/kb-categories.yaml` — 5 个分类元数据（数学📐、文学📚、历史🏛️、哲学🧠、科学🔬）
- `layouts/index.html` — 知识库首页（分类卡片网格 + 最近更新列表）
- `layouts/_default/section.html` — 智能 section 模板（有子 section 显示人物卡片，无子 section 显示文章列表）
- `layouts/_default/author.html` — 作者详情页模板（照片 + 生平 + 成就 + 作品列表）
- `layouts/partials/header.html` — 覆盖主题 header，导航栏改为 KB 分类链接
- 示例内容：`content/literature/`（王小波、闫连科两位作者 + 2 篇作品评价）
- 空分类 section：`content/history/`、`content/philosophy/`、`content/science/`

### 修改文件
- `hugo.toml`：mainSections 增加 5 个分类、permalinks 增加 `:sections` 规则、description 更新
- `content/math/_index.md`：title 从"数学题解"改为"数学"

### 关键技术点
- Hugo 模板覆盖：根目录 `layouts/` 优先于 `themes/weisaygrace/layouts/`
- `layout: author` frontmatter 触发 `layouts/_default/author.html` 模板
- `.Sections` 判断 section 是否有子 section（人物），`.Pages` 获取文章列表
- `:sections` permalink 变量生成嵌套 URL（如 `/literature/wang-xiaobo/slug.html`）
- `(index .Site.Data "kb-categories")` 访问带连字符的数据文件名
- `print . " 年生"` 替代 `printf "%s"` 处理 YAML 数值类型
- CSS 内联在模板中，复用主题 CSS 变量（`--color_link`、`--color_border` 等），自动适配暗色模式

## 数学分类子专题卡片化

将数学分类从平铺文章列表改为专题卡片网格，与文学分类的作家卡片结构一致。

### 改动
- `layouts/_default/section.html` 更新：
  - 添加 emoji `icon:` frontmatter 字段支持（专题卡片显示 emoji 而非首字母）
  - 替换 `breadcrumbs.html` partial 为自定义多级面包屑（子专题页显示「首页 > 数学 > 2026高考真题」）
  - 卡片计数改用 `.RegularPages` 确保只统计文章
  - 修复 `alt` 属性和默认描述的上下文问题
- 新增 4 个数学子专题目录及 `_index.md`：
  - `content/math/2026-gaokao/` — 📝 2026高考真题（7篇）
  - `content/math/mock-exam/` — 📋 模拟试题（4篇）
  - `content/math/problem-analysis/` — ✏️ 题型解析（5篇）
  - `content/math/teaching/` — 💡 教学随笔（3篇）
- 19 篇数学文章从 `content/math/` 移入对应子专题目录

### 关键验证
- 文章 URL 完全不变（permalink `math = '/archives/:slug.html'` 基于 slug，与目录位置无关）
- 首页数学分类计数仍为 19（`.Site.RegularPages` 包含子目录中的文章）
- 首页「最近更新」仍正常显示数学文章
- 任何分类都可用同样方式创建子专题：只需建子目录 + `_index.md`（含 `icon:` 字段）

## 新增「生活」分类 + 改名「平哥知识库」+ 修复 admin 兼容性

### 改名
- `hugo.toml` title 从 `平哥数学` → `平哥知识库`，logo/description/keywords 同步更新
- admin index.html 品牌名从 `平哥解数学` → `平哥知识库`（登录页 + 侧边栏）
- admin 域名链接从 `math.pgoj.top` → `www.pgoj.top`

### 新增「生活」分类
- `data/kb-categories.yaml` 增加 life（🌿，order: 6）
- `content/life/_index.md` 创建
- `hugo.toml` mainSections 和 permalinks 增加 life

### admin 后台兼容性修复（关键！）
原有 admin 不兼容知识库改造后的子目录结构，主要问题：
1. **listPosts 只扫描扁平目录** — 数学文章移入子目录后，admin 列出 0 篇
2. **getPost/updatePost/deletePost 路径构造不含子目录** — 编辑/删除失败
3. **分类下拉只有 math/posts** — 缺少其他分类
4. **getTaxonomies 只扫描扁平目录 + 域名硬编码 math.pgoj.top**

修复方案：
- **worker.js**: listPosts 递归扫描子目录，返回 `subSection` 字段；getPost/updatePost/deletePost/createPost 支持 `subsection` 查询参数；getTaxonomies 递归扫描 + 域名改 www.pgoj.top
- **index.html**: 分类下拉增加全部 6 个分类；editPost/savePost/deletePost 传递 `subsection` 参数；分类/标签中文映射更新
- ⚠️ **worker.js 需重新部署到 Cloudflare Workers** 才能生效（通过 Dashboard 或 wrangler）

## 改名「平哥集录」+ admin 新增专题管理功能

### 改名
- `hugo.toml` title/logo/description 从「平哥知识库」→「平哥集录」
- admin index.html 登录页副标题和侧边栏品牌名同步更新

### admin 新增「专题」Tab — 可在后台直接管理专题卡片
- **worker.js 新增 API**：
  - `GET /api/subsections?section=math` — 递归列出子专题目录，解析 _index.md 元数据（title/description/icon/layout/image/birth/death/era/achievements + 文章数）
  - `POST /api/subsection` — 创建新子专题（目录 + _index.md）
  - 复用 `GET /api/post/_index.md?section=X&subsection=Y` 获取内容
  - 复用 `PUT /api/posts/_index.md?section=X&subsection=Y` 更新内容
  - 辅助函数：extractFMValue、extractFMArray、extractBody
- **index.html 新增前端**：
  - 侧边栏新增「专题」导航项（在文章和页面之间）
  - 专题列表页：分类选择器 + 卡片网格（图标+名称+描述+文章数）
  - 专题编辑器：表单式（名称/目录名/描述/图标/布局/正文），布局选 author 时展开作者字段（照片/生卒年/年代/成就）
  - 新建专题时目录名可编辑，编辑时锁定
- ⚠️ worker.js 需重新部署到 Cloudflare Workers 才能生效

## 推送成功
- 解决 git push 被拒问题：远程有新提交，先 commit 8 篇未暂存的新文章（68058cd），再 `pull --rebase`，最后 `push origin main` 成功（0ab7f69）
- Cloudflare Pages 应已自动触发构建部署

## 通过 GitHub PAT 推送（16:20）

- 使用 classic PAT (`ghp_`) 成功推送 commit `dba442e`（admin 编辑器重构 + 首页移除 hero 标题 + header 修复）
- 推送方式：`git push https://user:token@github.com/...`（因为无 TTY 无法交互认证）
- 推送 5 个文件：kb-categories.yaml(2行)、index.html(4行)、admin/index.html(297行)、worker.js(62行)、header.html(5行)
- Cloudflare Pages 自动构建部署到 pgkb.pgoj.top

## 后续修复与部署
- 通过 GitHub PAT (`ghp_ZZWB...`) 成功推送本地 commit `dba442e` 到 GitHub main 分支
- **Worker 部署**：通过 `npx wrangler login` + `wrangler deploy` 将最新 `worker.js` 部署到 Cloudflare Workers (blog-admin-api, api.pgoj.top)
- Worker 更新内容：checkOrigin 添加 `origin.endsWith('.pgoj.top')` 支持、isLocalDev()、getTaxonomies 移除 taxonomies.json 依赖
- wrangler 登录方式：OAuth 浏览器授权（dash.cloudflare.com）
- **注意**：Cloudflare Dashboard 中 workers_dev=false 的 Worker 不在常规列表中显示明显入口，需用 wrangler CLI 管理
- **Bug 修复（dd2dd64）**：`loadTaxonomyHints()` 接收 subsections API 响应时，Worker 返回的是纯数组 `[...]` 但前端取的是 `subData.subsections`（undefined），导致 `_allSubSections` 始终为空。修复为 `Array.isArray(subData) ? subData : (subData.subsections || [])`。同时修复 `renderSubChips` 字段名 `s.count` → `(s.articleCount || s.count || 0)`
- 首页移除副标题描述文字；导航栏 header 加 `white-space: nowrap` 防止分类名竖排显示
- **Admin worker.js 三次部署**：
  1. 版本 `08f7128b` — 专题 API（listSubSections/createSubSection）上线
  2. 版本 `4864ce93` — **修复分类/标签选择器为空 bug**
     - 根因：`getTaxonomies()` 对 math 分类依赖不存在的 `/taxonomies.json`（Hugo 未生成），返回 404 HTML → 编辑器 chip 为空
  3. 版本 `38aa894f` — **修复 pgkb.pgoj.top 域名 403 问题**
     - 根因：线上 Worker 是旧版代码，`checkOrigin` 只允许 `www.pgoj.top` 精确匹配，`pgkb.pgoj.top` 的 Origin 被拒返回 403
     - 修复：本地最新 worker.js 已有 `origin.endsWith('.pgoj.top')` 兜底，通过 wrangler deploy 部署上线

## 编辑器移动端布局 + baseURL 修复（19:20）

### 问题1：移动端编辑器元数据字段不整齐
- 现象：334px 宽度下日期/分类/专题/slug/封面等字段出外进不对齐
- 根因：`.editor-fields` 使用 `align-items: center` + `flex-wrap:wrap`，换行后各行高度不一致导致错位
- 修复：
  - CSS 类 `.editor-fields` 改为 `align-items:flex-start`
  - 内联 style 同步修改（去掉 center → flex-start）
  - 移动端媒体查询 `<600px` 增强：强制 `.editor-fields > div` 和 `.meta-input` 为 `width:100%!important`，确保窄屏下垂直堆叠

### 问题2：专题链接域名错误
- 现象：专题链接显示 `https://math.pgoj.top/math/2026-gaokao/`
- 根因：`hugo.toml` 中 `baseURL = 'https://math.pgoj.top/'` 未更新
- 修复：改为 `baseURL = 'https://pgkb.pgoj.top/'`
- commit `af68ff8` 已推送至 GitHub

## 主题全面优化（20:30）

### 高优先级修复
1. **index.json 搜索索引修复**：只索引 "posts" section → 改为索引所有 mainSections（math/literature/history/philosophy/science/life），搜索功能恢复正常
2. **MathJax 条件加载**：所有页面都加载 ~1MB MathJax → 仅 `math: true` 的文章才加载，非数学页面性能提升
3. **清理 search/single.html debug 代码**：移除生产环境中的 console.log、黄色背景 debug CSS、DEBUG HTML 注释
4. **Waline 改为 CDN 加载**：192KB 本地 waline.js → CDN 压缩版（unpkg.com/@waline/client），CSS 也改为 preload 异步加载

### 中优先级修复
5. **dark.css 清理 WordPress 遗留死代码**：移除 .comment-*、.wp-block-*、.calendar_wrap、.vip1-.vip12、.fenye、.right-side、.article-related、.link-blogger 等未使用样式，文件 21KB → 18KB
6. **main.js 移除死代码**：删除 dark-mode-toggle（桌面端按钮）的事件监听，该按钮已不存在
7. **删除未使用静态资源**：iconfont.eot(165KB)、404.png(26KB)、manifest.json
8. **robots.txt 清理**：移除 /wp-content/、/wp-admin/、/vendor/、/node_modules/ 等 WordPress 路径
9. **hugo.toml 清理**：删除已注释的 Twikoo 配置

### 跳过项
- reading.html：设计是故意定制的（阅读页面特殊样式），不算重复代码
- sidebar.html：死代码但未删除（296行，可能未来会用到）

### 提交信息
- commit `389cc66`：所有优化已提交到本地 git
- 推送失败（网络连接重置），需手动推送到 GitHub 触发 Cloudflare Pages 构建

## Admin 文章列表不显示非日期命名文件（21:20）

### 问题现象
文学分类下《黄金时代》评价和《白银时代》评价两篇文章在管理后台文章列表中不可见

### 根因
`loadPosts()` 中文件名过滤逻辑 `p.name.match(/^(\d{4})-(\d{2})-/)` 要求文件名以 `YYYY-MM-` 开头，不符合的直接 `return` 跳过。这两篇文章使用 slug 命名（`huangjin-shidai.md`、`baiyin-shidai.md`），被静默丢弃。

### 修复内容（commit `ce753ed`）
1. 不匹配日期模式的文件收集到 `otherPosts` 数组而非跳过
2. 根目录导航增加「📄 其他 (N)」按钮
3. 新增 `_other/` prefix 处理分支，显示非日期命名的扁平文章列表
4. 推送成功至 GitHub
